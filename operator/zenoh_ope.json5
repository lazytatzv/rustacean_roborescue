{
  mode: "peer", // or "client" if using a router
  connect: { endpoints: ["quic/10.42.0.1:7447"] }, // Robot IP (change as needed)
  scouting: { multicast: { enabled: false } },
  transport: { shared_memory: { enabled: true } }
}
