# 操作マニュアル

## 目次

1. [環境構築](#1-環境構築)
2. [ビルド](#2-ビルド)
3. [実機起動](#3-実機起動)
4. [シミュレーション](#4-シミュレーション)
5. [コントローラ操作](#5-コントローラ操作)
6. [自律走行 (Nav2)](#6-自律走行-nav2)
7. [シリアルデバイス設定](#7-シリアルデバイス設定)
8. [ネットワーク構成](#8-ネットワーク構成)
9. [本番デプロイ](#9-本番デプロイ)
10. [トラブルシューティング](#10-トラブルシューティング)

---

## 1. 環境構築

### 1.1 前提条件

- **OS**: Linux (NixOS / Ubuntu 等)
- **Nix**: バージョン 2.18 以上 (flakes 有効)
- **GPU**: Gazebo GUI を使う場合は OpenGL 3.3 以上 (nixGL 経由)

### 1.2 初回セットアップ

```bash
# リポジトリクローン (サブモジュール含む)
git clone --recursive <REPO_URL>
cd rustacean_roborescue

# サブモジュール取得 (クローン時に --recursive を忘れた場合)
just sync
# または: git submodule update --init --recursive
```

### 1.3 Nix 開発シェルに入る

```bash
# flake.nix で定義された全ツール (ROS 2 Jazzy, Rust nightly, Gazebo Harmonic 等) が使える
just nix
# または: nix develop --impure
```

> **`--impure` が必要な理由**: nixGL が `/run/opengl-driver` 等のホスト側 GPU ドライバを参照するため。

起動すると以下が表示される:

```
=======================================================
 Rustacean RoboRescue Dev Environment
 ROS: jazzy | Rust: nightly | Sim: Gazebo Harmonic
=======================================================
```

---

## 2. ビルド

### 2.1 フルビルド (推奨)

```bash
# Nix シェル内で実行
cd main_ws

# フルビルド (Rust バインディング生成 → 全パッケージ)
just forge
```

### 2.2 クリーンビルド

```bash
cd main_ws
just reforge   # build/ install/ log/ を削除してからフルビルド
```

---

## 3. 実機起動

### 3.1 全ノード一括起動

```bash
cd main_ws
source install/setup.bash

# 全系統を一括起動
ros2 launch bringup system.launch.py
```

### 3.2 アーム単体テスト

```bash
# アーム関連ドライバとコントローラのみ起動
ros2 launch bringup test_arm.launch.py
```

---

## 5. コントローラ操作

PS4 (DualShock 4) コントローラで操作する。
4 つの動作モードがあり、ボタンで切り替える。

### 5.1 モード切替

| ボタン | モード | 説明 |
|--------|--------|------|
| **PS** | STOP | **緊急停止** (E-Stop): 全モータ停止、ラッチ式 |
| **OPTIONS** | DRIVE | クローラ＋フリッパ操作 |
| **SHARE** | **ARM / JOINT** | アーム操作。押すごとに IK制御(ARM) と 関節個別制御(JOINT) を切り替え |

> 起動直後は **STOP** モード。
> モード切替ボタンはすべてエッジ検出 (押した瞬間のみ反応)。

### 5.2 DRIVE モード (走行)

| 入力 | 動作 |
|------|------|
| 左スティック ↑↓ | 左クローラ 前進/後退 |
| 右スティック ↑↓ | 右クローラ 前進/後退 |

#### フリッパ操作

| 入力 | フリッパ | 方向 |
|------|----------|------|
| D-Pad ↑ | 左後フリッパ | + |
| D-Pad ↓ | 左後フリッパ | − |
| × ボタン | 右後フリッパ | + |
| □ ボタン | 右後フリッパ | − |
| L1 | 左前フリッパ | + |
| L2 (深く引く) | 左前フリッパ | − |
| R1 | 右前フリッパ | + |
| R2 (深く引く) | 右前フリッパ | − |

### 5.3 ARM モード (逆運動学 IK 制御)

手先の移動速度を直感的に操作するモード。

| 入力 | 動作 | スケール |
|------|------|----------|
| 左スティック ↑↓ | 手先 前後 (X) | 0.3 m/s |
| 左スティック ←→ | 手先 左右 (Y) | 0.3 m/s |
| 右スティック ↑↓ | 手先 上下 (Z) | 0.3 m/s |
| 右スティック ←→ | 手先 Yaw 回転 | 0.5 rad/s |
| L2 / R2 | Roll 回転 (R2で正, L2で負) | 0.5 rad/s |
| D-Pad ↑↓ | Pitch 回転 (↑で正, ↓で負) | 0.5 rad/s |

> **特異点ロック**: アームが伸び切るなどして `Singularity lockout!` ログが出た場合は、JOINTモードに切り替えて姿勢を崩してください。

### 5.4 JOINT モード (関節個別操作)

各関節を個別に動かすモード。IKが通らない姿勢からの脱出に使用する。

| 入力 | 動作対象 |
|------|----------|
| 左スティック ↑↓ | Joint 1 (旋回台) |
| 左スティック ←→ | Joint 2 |
| 右スティック ↑↓ | Joint 3 |
| 右スティック ←→ | Joint 4 |
| L2 / R2 トリガー | Joint 5 (L2で負, R2で正) |
| D-Pad ↑↓ | Joint 6 (↑で正, ↓で負) |

---

## 7. シリアルデバイス設定

| デバイス | デフォルトパス | ノード | ボーレート |
|----------|----------------|--------|-----------|
| Roboclaw | `/dev/roboclaw` | crawler_driver | 38400 |
| STM32 (BNO055) | `/dev/stm32` | sensor_gateway | 115200 |
| アーム (U2D2) | `/dev/ttyUSB0` | arm_driver | 1000000 |
| グリッパ (U2D2) | `/dev/ttyUSB1` | gripper_driver | 115200 |
| フリッパ (U2D2) | `/dev/dynamixel` | flipper_driver | 115200 |

---

## 10. トラブルシューティング

### 10.6 アームが動かない

1. **`arm_driver` のログを確認**:
   - `Pinging motors [21, 22, ...]` で失敗していないか。
   - `📊 Arm Positions:` が毎秒表示されているか（表示されていれば通信は正常）。
2. **`arm_controller` のログを確認**:
   - `Singularity lockout!` が出ていないか。出ていれば JOINT モードで姿勢を変える。
   - `Waiting for /joint_states` で止まっていないか（ドライバからのフィードバック待ち）。
3. **モードの確認**:
   - `SHARE` ボタンを押して `Mode: ARM` または `Mode: JOINT` と出ているか。
