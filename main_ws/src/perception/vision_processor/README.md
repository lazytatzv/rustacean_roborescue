## colcon cargo intro

```bash
pip install colcon-cargo colcon-ros-cargo 
```

system dependencies

```bash
sudo apt update
sudo apt install -y \
    libopencv-dev \
    libopencv-contrib-dev \
    clang \
    libclang-dev \
    build-essential
```
