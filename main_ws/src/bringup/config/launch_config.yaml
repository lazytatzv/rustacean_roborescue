# ロボット起動設定
# どのサブシステムを有効にするかをここで制御する。
# ros2 launch bringup system.launch.py で読み込まれる。
# 単一高可用構成を優先するため、起動時の細かい切替よりこのファイルを正として運用する。

use_crawler: true
use_arm: false
use_flipper: false
use_imu: false
use_lidar: true
use_camera: true
use_nav2: false
use_audio: true
