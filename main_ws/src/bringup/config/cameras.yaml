# カメラ設定リスト
# カメラを追加する場合はここにエントリを追加するだけ。
# namespace は "camera_<name>" になる (例: camera_front, camera_arm)
#
# 共通フィールド:
#   name        : カメラ識別名 (namespace に使用)
#   driver      : v4l2 | realsense  (デフォルト: v4l2)
#   use_qr      : QR コード検出を有効にするか (デフォルト: false)
#   image_topic : QR検出・ffmpegが購読するトピック名 (namespace相対)
#                 v4l2 → image_raw / realsense → color/image_raw
#
# v4l2 固有:
#   device      : v4l2 デバイスパス
#   width/height: 解像度
#   fps         : フレームレート
#   frame_id    : TF フレーム名
#   encoder     : ffmpeg エンコーダ (libx264 / h264_v4l2m2m など)
#   bitrate     : エンコードビットレート [bps]
#
# realsense 固有:
#   serial      : シリアル番号 (空文字 = 自動検出)
#   enable_color: カラーストリーム有効 (デフォルト: true)
#   enable_depth: 深度ストリーム有効 (デフォルト: false)
#   enable_pose : T265 オドメトリ有効 (デフォルト: false)
#   color_width/color_height/color_fps: カラー解像度
#   depth_width/depth_height/depth_fps: 深度解像度

cameras:
  - name: front
    driver: v4l2
    device: /dev/video0
    width: 640
    height: 480
    fps: 30
    frame_id: front_camera_link
    use_qr: false
    encoder: libx264
    bitrate: 2000000

  # ── RealSense D435 (アームカメラ) の例 ────────────────────────────────────
  # - name: arm
  #   driver: realsense
  #   serial: ""            # 複数台使う場合はシリアル番号を指定
  #   enable_color: true
  #   enable_depth: true
  #   color_width: 640
  #   color_height: 480
  #   color_fps: 30
  #   depth_width: 640
  #   depth_height: 480
  #   depth_fps: 30
  #   use_qr: true          # カラー画像でQR検出
  #   encoder: libx264
  #   bitrate: 1000000

  # ── RealSense T265 (トラッキングカメラ) の例 ──────────────────────────────
  # - name: tracking
  #   driver: realsense
  #   serial: ""
  #   enable_color: false   # fisheye のみ、通常カラーなし
  #   enable_pose: true     # /camera_tracking/pose/sample を配信
  #   use_qr: false
