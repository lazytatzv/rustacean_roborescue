# arm_driver parameters
arm_driver:
  ros__parameters:
    port_name: "/dev/ttyUSB_arm"
    baud_rate: 115200
    # Profile velocity: limits max servo speed during position moves
    # Unit: 0.229 rev/min per LSB. 100 ≈ 22.9 RPM ≈ 2.4 rad/s
    profile_velocity: 100
