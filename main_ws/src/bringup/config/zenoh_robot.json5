{
  mode: "client",
  connect: { endpoints: ["quic/127.0.0.1:7447"] },   // local zenohd (QUIC)
  scouting: { multicast: { enabled: false } },         // Wi-Fi へのマルチキャスト漏れ防止
  transport: { shared_memory: { enabled: true } }       // SHM (同一マシン内高速転送)
}
