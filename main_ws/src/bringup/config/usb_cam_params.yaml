# usb_camノードは一旦使わず、v4l2を使う予定
# h264でエンコードするため

usb_cam:
  ros__parameters:
    video_device: "/dev/video0"
    framerate: 30.0
    image_width: 640
    image_height: 480
    pixel_format: "mjpeg" # 高速化の鍵
    brightness: -1        # 自動
    camera_name: "front_camera"
